1. Build Model for comments called Class Comment.
2. Create ForeignKey to link to Article/Topic and add on_delete=models.CASCADE so that if a article is removed then the post is removed from the DB.
3. Add related_name as a variable to reference later.
4. Add fields/values you want to include in comment (e.g. name, body, rating)
5. Add def __str__ ref to reference the model in a human-readable way.
6. Import and Register model in admin so that it can be manipulated in admin console. 